# =============================================================================
# MONCOYFINANCE - VARIÁVEIS DE AMBIENTE
# =============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo e renomeie para: .env.local
# 2. Preencha os valores abaixo com suas credenciais
# 3. NUNCA commite o .env.local no Git (já está no .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE (Banco de Dados e Autenticação)
# -----------------------------------------------------------------------------
# Onde encontrar:
# 1. Acesse: https://supabase.com/dashboard
# 2. Selecione seu projeto
# 3. Vá em: Settings > API
# 4. Copie "Project URL" e "anon public"

NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key (OPCIONAL - apenas para operações de admin no servidor)
# ⚠️ CUIDADO: Nunca exponha essa key no client-side!
# Encontre em: Settings > API > service_role key (só visível com senha)
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...


# -----------------------------------------------------------------------------
# OPENAI (Inteligência Artificial)
# -----------------------------------------------------------------------------
# Onde encontrar:
# 1. Acesse: https://platform.openai.com/api-keys
# 2. Faça login ou crie conta
# 3. Clique em "Create new secret key"
# 4. Copie a key (só é mostrada uma vez!)

OPENAI_API_KEY=sk-proj-...


# -----------------------------------------------------------------------------
# STRIPE (Pagamentos e Assinaturas)
# -----------------------------------------------------------------------------
# Onde encontrar:
# 1. Acesse: https://dashboard.stripe.com/test/apikeys (modo teste)
# 2. Ou: https://dashboard.stripe.com/apikeys (modo produção)
# 3. Copie "Publishable key" e "Secret key"

# Chave pública (pode ser exposta no client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Chave secreta (apenas server-side)
STRIPE_SECRET_KEY=sk_test_...

# Webhook Secret (para verificar eventos do Stripe)
# Encontre em: Developers > Webhooks > [seu endpoint] > Signing secret
STRIPE_WEBHOOK_SECRET=whsec_...


# -----------------------------------------------------------------------------
# URLs DO SITE
# -----------------------------------------------------------------------------
# Configure de acordo com seu ambiente

# Desenvolvimento Local
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Produção (descomente quando deployar)
# NEXT_PUBLIC_SITE_URL=https://moncoyfinance.com


# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# -----------------------------------------------------------------------------

# Modo de produção (descomente em produção)
# NODE_ENV=production

# Vercel (automaticamente setado pelo Vercel)
# VERCEL=1

# Analytics (se usar Vercel Analytics)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=...


# -----------------------------------------------------------------------------
# GOOGLE OAUTH (Opcional - para login social)
# -----------------------------------------------------------------------------
# Configure no Supabase Dashboard em: Authentication > Providers > Google
# Não precisa de variáveis aqui, configure direto no Supabase

# Se quiser usar Google OAuth:
# 1. Vá para: https://console.cloud.google.com/
# 2. Crie um projeto ou selecione existente
# 3. Vá em: APIs & Services > Credentials
# 4. Crie OAuth 2.0 Client ID
# 5. Configure no Supabase (não no .env)


# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 
# ✅ DESENVOLVIMENTO:
# - Use keys de TESTE do Stripe (pk_test_... e sk_test_...)
# - Use NEXT_PUBLIC_SITE_URL=http://localhost:3000
# 
# ✅ PRODUÇÃO:
# - Use keys de PRODUÇÃO do Stripe (pk_live_... e sk_live_...)
# - Use NEXT_PUBLIC_SITE_URL=https://moncoyfinance.com
# - Configure todas as variáveis no Vercel/sua plataforma
# 
# ⚠️ SEGURANÇA:
# - NUNCA commite .env.local no Git
# - NUNCA exponha STRIPE_SECRET_KEY ou SUPABASE_SERVICE_ROLE_KEY
# - Variáveis com NEXT_PUBLIC_ são expostas no navegador
# - Variáveis sem NEXT_PUBLIC_ são apenas server-side
# 
# 🔄 ATUALIZANDO KEYS:
# - Após alterar .env.local, reinicie o servidor (npm run dev)
# - No Vercel, atualize em: Settings > Environment Variables
# 
# =============================================================================
